@model IEnumerable<DonorTrackingSystem.Models.NonCongregant>
@{
    ViewData["Title"] = "View Non-Congregant Donors";
}

<!-- Container for viewing all non-congregant donors -->
<div class="container mt-4">
    <!-- Page header -->
    <div class="row mb-3">
        <div class="col-12">
            <h2>Non-Congregant Donors</h2>
            <hr />
        </div>
    </div>

    <!-- Success message notification -->
    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <!-- Navigation buttons -->
    <div class="d-flex justify-content-between mb-3">
        <!-- Button to add a new non-congregant donor -->
        <a asp-action="AddNonCongregant" class="btn btn-primary">
            <i class="bi bi-plus-circle"></i> Add Non-Congregant Donor
        </a>
        <!-- Button to return to dashboard -->
        <a asp-action="Index" class="btn btn-secondary">
            <i class="bi bi-house"></i> Back to Dashboard
        </a>
    </div>

    <!-- Main non-members table -->
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-body">
                    <!-- Make table scrollable on small screens -->
                    <div class="table-responsive">
                        <table class="table table-striped table-hover">
                            <thead class="table-dark">
                                <tr>
                                    <th>ID</th>
                                    <th>Name</th>
                                    <th>Company/Organization</th>
                                    <th>Contact Details</th>
                                </tr>
                            </thead>
                            <tbody>
                                <!-- Check if there are non-members to display -->
                                @if (Model != null && Model.Any())
                                {
                                    <!-- Loop through each non-member -->
                                    @foreach (var nonMember in Model)
                                    {
                                        <tr>
                                            <td>@nonMember.ID</td>
                                            <td>
                                                @if (!string.IsNullOrWhiteSpace(nonMember.FirstName) || !string.IsNullOrWhiteSpace(nonMember.LastName))
                                                {
                                                    <!-- Display full name if available -->
                                                    <span>@nonMember.FirstName @nonMember.LastName</span>
                                                }
                                                else
                                                {
                                                    <!-- Show dash if no name -->
                                                    <span class="text-muted">-</span>
                                                }
                                            </td>
                                            <td>
                                                @if (!string.IsNullOrWhiteSpace(nonMember.CompanyOrganization))
                                                {
                                                    <!-- Display company/org if available -->
                                                    <span class="badge bg-info">@nonMember.CompanyOrganization</span>
                                                }
                                                else
                                                {
                                                    <!-- Show dash if no company -->
                                                    <span class="text-muted">-</span>
                                                }
                                            </td>
                                            <td>
                                                @if (!string.IsNullOrWhiteSpace(nonMember.ContactDetails))
                                                {
                                                    <!-- Truncate long contact details -->
                                                    @(nonMember.ContactDetails.Length > 50 
                                                        ? nonMember.ContactDetails.Substring(0, 50) + "..." 
                                                        : nonMember.ContactDetails)
                                                }
                                                else
                                                {
                                                    <!-- Show dash if no contact info -->
                                                    <span class="text-muted">-</span>
                                                }
                                            </td>
                                        </tr>
                                    }
                                }
                                else
                                {
                                    <!-- Message when no non-members exist -->
                                    <tr>
                                        <td colspan="4" class="text-center">No non-member donors found.</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Summary card (only shown if non-members exist) -->
    @if (Model != null && Model.Any())
    {
        <div class="row mt-3">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Summary</h5>
                        <!-- Count total number of non-member donors -->
                        <p><strong>Total Non-Member Donors:</strong> @Model.Count()</p>
                        <p><strong>Individual Donors:</strong> @Model.Count(n => !string.IsNullOrWhiteSpace(n.FirstName) || !string.IsNullOrWhiteSpace(n.LastName))</p>
                        <p><strong>Organization Donors:</strong> @Model.Count(n => !string.IsNullOrWhiteSpace(n.CompanyOrganization))</p>
                    </div>
                </div>
            </div>
        </div>
    }
</div>
